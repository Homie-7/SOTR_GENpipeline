# S9-SAL-R-SNUFF · v7 · JOIN SOLVED (Sequel) · smoke awaiting Homie · 2026-09-24 (Claude, connector)
#
# v7 RESULT: the snuff continues the loop natively. 0 px, every region within 2% at the join,
#   invisible in a single loop+snuff file. Staging right in both takes. Smoke came out PALE
#   against a darker dusk and still rising at 5 s (A puffy, B thin threads); Homie's web take
#   had dark threads that cleared by 4 s. THIS IS THE ACTIVE ROUTE for any further snuff run.
#
# WHY v7: Homie, 2026-09-24: "We are not going to rely on QLab or any projection software
#   whatsoever." The join from the lit loop into the snuff must be correct INSIDE the file.
#   v5/v6 proved this connector can't pin a first frame. SEQUEL MODE CAN CONTINUE A VIDEO:
#   `video_extension` forward, from the approved comp loop, so the snuff starts where the loop
#   ends by construction.
# CHANGES vs v4 (one design move): the reference mechanism, and the two lines that described
#   the old one. Every other word is v4.
#   REFERENCE  v4: "@loc_SOTR_salon_R_s9_v1 is the seed frame and controls the whole picture: ..."
#              v7: "This shot continues the attached clip of the same salon wall directly from
#                   its last frame, and keeps the whole picture: ..."
#   FIRST FRAME v4: "Identical to the reference: ..."
#              v7: "Continues seamlessly from the last frame of the attached clip: ..."
# SETTINGS · Seedance 2.5 · mode video_extension, extension_mode forward · video reference =
#   SOTR_MEDIA/refs/SAL-R-LOOP_comp_upload.mp4 (H.264 crf 10 of loc_SOTR_salon_R_loop_s9_v1_comp.mov,
#   sha256 0ab33cc4…, media 4afb0ccd-af6f-4d79-9255-f099561f1c5e) · aspect follows the input (4:3) ·
#   1080p · 5 s (Homie's 5 s web take was complete) · bitrate_mode high · batch 2 · sound ON ·
#   60 credits per take, 120 total
# PASS = the new footage starts on the loop's last frame (<=1 px, a few levels), and the exit
#   stages as before (left, then right, no smoke while lit, threads not puffs).
#
# ---- v6 header below ----
#
# S9-SAL-R-SNUFF · v6 REFUSED BY THE SERVER (422, no charge) · 2026-09-24
#
# v6 = v4's body WORD FOR WORD. ONE CHANGE against v5: the mode. v5 sent `start_image` in
#   mode omni_reference and the server coerced it to a reference. v6 leaves `mode` unset
#   (server default, t2v), which is where a first-frame role normally lives. The same start
#   image: SOTR_MEDIA/refs/SAL-R-LOOP_comp_f0.png (media af61efa9-…).
#   SETTINGS · Seedance 2.5 · mode default · start_image as above · 4:3 · 1080p · 4 s ·
#   bitrate_mode high · batch 1 · sound ON · 48 credits.
#   PASS = frame 0 within ~1 px and a few levels of the comp loop, and the echo does not say
#   reference_images.
#
# HOMIE'S WEB TAKE (found 2026-09-24): SOTR_MEDIA/loops/S9-SAL-R-SNUFF_v4_web5s.mp4 (his "Snuff not
#   bad", sha256 4a63d26b…). 5.05 s, 121 frames: his own web run just before this session
#   (-60 credits, 03:03 UTC), prompt presumed v4 (UNCONFIRMED). The best take so far: left out
#   0.79 s, right 1.71 s, thin dark threads, frame 0 only 4 px off the loop, halos within 5%,
#   but the dado -22%, far-left panel -16% and portrait -27% darker.
#
# FOUND 2026-09-24: v4's "no comp step" is WRONG. Every generated clip carries the model's
#   placeholder king, and Gerard's portrait is composited over the lit loop (LOOK.md World 1).
#   A raw cut from the loop to this clip would swap the painting. The snuff needs the
#   S9-SAL-R-PORTRAIT comp too, with Gerard darkening as the sconces go out. Flagged for Homie.
#
# ---- v5 header below ----
#
# S9-SAL-R-SNUFF · v5 FAILED (connector coerced the role) · ACTIVE SETTINGS = v4's · 2026-09-24
#
# RESULT: v5 FAILED ON THE MECHANISM, not the prompt. In mode omni_reference the server
#   silently turned `start_image` into an ordinary reference (its echo says reference_images).
#   Frame 0 came back as a NEW render: 25 levels darker than the loop, shifted 9-10 px, the
#   room pulled back. Worse than v4. Both takes are in rejected/.
#   The body is identical in v4 and v5, so all four takes sample the same wording: smoke came
#   back 2 x dark curling threads (v4 A, v5 A) and 2 x pale puffs and haze (v4 B, v5 B).
#   The smoke is a coin toss. Staging: 4 of 4 left-then-right, no smoke while lit.
#   NEXT is Homie's call (see docs/NEW-SESSION.md): how to make frame 0 match the loop.
#   Until then the active settings are v4's (lit plate as image_references).
#
# v5 = v4's body WORD FOR WORD. ONE CHANGE, in the reference only:
#   v4: the lit PLATE as a loose reference (`image_references`, the web's References mode).
#   v5: frame 0 of the approved COMP LOOP as the clip's FIRST FRAME (`start_image`, an API
#       role the web menu doesn't show). File: SOTR_MEDIA/refs/SAL-R-LOOP_comp_f0.png
#       (sha256 831918…, Higgsfield media af61efa9-ca18-4539-860e-93c7d4b705f8).
#   WHY: v4 probe A staged the exit exactly right (left out at 1.00 s, right at 2.00 s) but its
#   frame 0 sat 16 px lower and ~11% darker than the lit loop, since every Seedance run
#   re-frames the wall by ~1%. On a hard QLab cut that's a visible jump of the whole wall. A
#   start image makes frame 0 the loop itself, so the cut matches by construction.
#   The @tag in the REFERENCE line is unchanged text (one change only).
#
# v5 SETTINGS (connector) · Seedance 2.5 · mode omni_reference · start_image =
#   SAL-R-LOOP_comp_f0.png · 4:3 · 1080p · PROBE 4 s · bitrate_mode high (= "High") ·
#   batch 2 · sound ON · 96 credits (48 per take)
#
# v4 PROBE RESULT (batch 2, 4 s, 96 credits): A = exit staged as written, thin dark curling
#   threads, each leading with a small curl; rises ~1 m (to the panel cartouche, higher than
#   asked). B = puff-headed pale plumes spreading to haze by 3.3 s, the v1-v3 defect (rejected).
#   Smoke: 1 of 2. Both: no smoke while lit, left before right, lock 0 px.
#
# ---- v4 header below, kept as written ----
#
# S9-SAL-R-SNUFF · v4 · 2026-09-24 (VID) · RESTRUCTURED, decided with Homie
#
# WHY v4 IS A RESTRUCTURE, NOT A REWORDING (lesson 6)
#   v1-v3 used the DARK plate as the reference and asked for "a moment after" the snuff.
#   All three STARTED LIT anyway, and the model staged the snuff itself. v2 and v3 then
#   streamed smoke off candles that were still burning. The smoke formed a puff-headed plume
#   every time: big clouds in v1, puff-balls in v3; v2's threads were close but it lost the
#   snuff. Two rewordings failed on the smoke, and the model rejected the premise three times.
#   The design was fighting what the model wants to show, so v4 gives it the event.
#   v1-v3 prompt text: git history (commit 3a6cd0b and earlier). Their clips are in rejected/.
#
# WHAT CHANGED (Homie, 2026-09-24)
#   1. REFERENCE = THE LIT PLATE. The clip IS the salon's exit: flames out, the room falls
#      to dusk, wisps. Its first frame matches the approved lit loop, so QLab cuts from the
#      loop straight into this clip on cue. No comp step. This bends CLAUDE.md's
#      "transitions are never generated" for this one in-world exit; the rule's reasons
#      (triggered on a cue, identical every time) still hold for a fixed clip.
#   2. ONE SCONCE AFTER THE OTHER: left, then right, a beat apart. Six flames out in the same
#      instant looked artificial in all three takes.
#   3. THE SMOKE NOUN CHANGED (lesson 3: when banning a defect by name doesn't hold, change
#      the noun). "Smoke rises" produced a plume with a head. "An incense-thin wisp" is a
#      continuous thread, with a visible landmark for where it ends: the top of the sconce's
#      carved back.
#   This is three changes, knowingly. A restructure after two failures is one design move,
#   and the probe below tells us whether it works.
#
# SETTINGS
#   Model:      Seedance 2.5
#   Mode:       References. Reference = @loc_SOTR_salon_R_s9_v1, the LIT plate
#               (SOTR_MEDIA/plates/salon/loc_SOTR_salon_R_s9_v1.jpg). Just this one.
#   Aspect:     4:3 (it must cut against the lit loop, which is 4:3)
#   Resolution: 1080p
#   Duration:   PROBE 4 s first. The whole snuff and the start of the wisps land inside 4 s,
#               which is where every earlier fault showed. CANDIDATE 10 s only once a probe
#               passes.
#   Quality:    High
#   Batch:      2 for the probe (is it the wording, or luck?), 1 for the candidate
#   Sound:      ON (a snuff hiss is a useful scratch for the sound designer)
#   Cost:       probe about 96 credits; candidate about 120
#
# JUDGE THE PROBE ON: the first frame matching the lit loop (a clean cut); left before right;
#   no smoke while any flame burns; wisps as unbroken threads with no puff head or cloud,
#   ending near the top of the sconce; the room settling to dusk; the portrait still.

SCENE: The approved salon wall at evening, all six candles burning, as they are put out one sconce after the other and the room sinks into dusk. One locked, continuous shot.

REFERENCE: This shot continues the attached clip of the same salon wall directly from its last frame, and keeps the whole picture: every panel, moulding, sconce, the portrait and its frame, the colour and the light, exactly as shown.

FIRST FRAME: Continues seamlessly from the last frame of the attached clip: all six candles burning, warm amber light.

FORMAT: Single continuous take, real-time, no cuts.

CAMERA: Locked off, square to the wall. No push, no pan, no tilt, no drift, no zoom.

ACTION TIMING:
0:00-0:01 — all six flames burn calmly.
0:01 — the three flames of the LEFT sconce go out together, snuffed; their warm glow on the panels falls away at once.
0:02 — the three flames of the RIGHT sconce go out the same way. With no flame left, the wall settles into the cool blue-grey dusk of an unlit room.
0:02-0:05 — from each dark wick a single incense-thin wisp of grey trails upward, one unbroken thread like the smoke from a stick of incense, wavering and curling, fading out around the top of the sconce's carved back.
0:05-end — the wisps thin and vanish. The wall stays in quiet dusk, the candles pale and unlit.

PHYSICS: Each wisp is continuous from the wick to where it fades, as thin at its top as at its start. It never gathers into a cloud or a puff.

LIGHTING: Warm candlelight until each sconce goes out, then only the cool blue-grey dusk of an unlit room.

AUDIO: The soft hiss of each snuffed wick, then quiet room tone. No music, no voices.

QUALITY: Stable, clean picture, real-time, no slow motion, no frame interpolation, no ghosting.

LOCKS:
NO ON-SCREEN TEXT.
The room is empty: nobody in frame.
Six flames at the start, none after 0:02; no smoke while any flame still burns.
LOCKED CAMERA.
Nothing in frame newer than 1819.
